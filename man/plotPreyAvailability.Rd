% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotPreyAvailability.R
\name{plotPreyAvailability}
\alias{plotPreyAvailability}
\alias{plotlyPreyAvailability}
\title{Plot the prey available to a predator of a given size}
\usage{
plotPreyAvailability(
  params,
  species = NULL,
  pred_size = NULL,
  return_data = FALSE
)

plotlyPreyAvailability(params, species = NULL, pred_size = NULL, ...)
}
\arguments{
\item{params}{A MizerParams object}

\item{species}{The name of the predator species. Optional if the model has
only a single species.}

\item{pred_size}{The weight of the predator in grams. Defaults to the
maturity weight \code{w_mat} of the species.}

\item{return_data}{A boolean value that determines whether the data frame
underlying the plot is returned instead of the plot itself. Defaults to
FALSE. The data frame has the variables \code{w}, \code{value} and \code{Type}.}

\item{...}{Additional arguments passed to \code{plotPreyAvailability()} by the
plotly wrapper.}
}
\value{
A ggplot2 object, or a data frame if \code{return_data = TRUE}. The
\code{plotly} version returns a plotly object.
}
\description{
Shows, for a predator of a given species and size, how the prey that it
encounters is distributed over prey sizes. Four curves are shown, each
normalised to integrate to 1 over log prey weight:
}
\details{
\itemize{
\item \strong{Feeding kernel}: the predation kernel \eqn{\phi(w_p/w)} alone, i.e.
the size preference of the predator, irrespective of what is there.
\item \strong{Number density}: the density of available prey individuals in prey
weight, i.e. the kernel times the abundance of the prey community.
\item \strong{Number density in log w}: the same, but as a density in log weight,
so that the area under the curve over a size range is proportional to
the number of available prey in that range.
\item \strong{Biomass density in log w}: the corresponding biomass density, so that
the area under the curve is proportional to the available prey biomass.
}

The prey community is the sum of the resource spectrum and the fish
spectra, each weighted by the predator's interaction strength with it,
see \code{\link[mizer:setInteraction]{mizer::setInteraction()}}. The size of the predator is marked by a
point on the x-axis.
}
\examples{
plotPreyAvailability(celtic_params, species = "Cod", pred_size = 1000)
}
\seealso{
\link[mizer:plotting_functions]{mizer::plotting_functions}, \code{\link[mizer:plotDiet]{mizer::plotDiet()}}

Other plotting functions:
\code{\link[=plotDeathX]{plotDeathX()}}
}
\concept{plotting functions}
