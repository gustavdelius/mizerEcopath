% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotAge.R
\name{plotAge}
\alias{plotAge}
\title{Plot mean age-at-length from model and survey data}
\usage{
plotAge(
  params,
  species,
  age_at_length,
  dt = 0.05,
  t_max = NULL,
  f_h = c(0.125, 0.625, 0.875),
  quarter_h = c(1, 3, 4),
  plot = c("mean", "quantiles")
)
}
\arguments{
\item{params}{A MizerParams object.}

\item{species}{Name of the species to plot.}

\item{age_at_length}{A data frame with columns \code{Scientific_name}, \code{Quarter},
\code{LngtClass}, \code{Age}, and \code{CANoAtLngt} giving observed age-at-length data.}

\item{dt}{Time step for the model simulation (years). Default is 0.05.}

\item{t_max}{Maximum age to simulate (years). Default is 6.}

\item{f_h}{Numeric vector of time of surveys in the different quarters.
Default c(0.125, 0.625, 0.875).}

\item{quarter_h}{Numeric vector of survey quarters. Default is
c(1, 3, 4).}

\item{plot}{One of "mean" or "quantiles". If "mean", the mean age-at-length
is plotted. If "quantiles", the 25\%, 50\% and 75\% quartiles are plotted.}
}
\value{
A ggplot2 object showing mean or quantiles of the age-at-length for
model and observed data, with 95\% confidence intervals for the observed
means.
}
\description{
This function compares the mean age-at-length predicted by the model to
observed survey data for a given species. It aggregates observed
age-at-length data into length bins, simulates a cohort using the model, and
plots the mean age-at-length with confidence intervals for the observed data.
}
\examples{
# See package vignette for example usage
}
