% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get.R
\name{getM0B}
\alias{getM0B}
\title{Get rate at which biomass is lost due to external mortality}
\usage{
getM0B(params, ...)
}
\arguments{
\item{params}{A MizerParams object}

\item{...}{
  Arguments passed on to \code{\link[mizer:get_size_range_array]{mizer::get_size_range_array}}
  \describe{
    \item{\code{min_w}}{Smallest weight in size range. Defaults to smallest weight in
the model.}
    \item{\code{max_w}}{Largest weight in size range. Defaults to largest weight in the
model.}
    \item{\code{min_l}}{Smallest length in size range. If supplied, this takes
precedence over \code{min_w}.}
    \item{\code{max_l}}{Largest length in size range. If supplied, this takes precedence
over \code{max_w}.}
  }}
}
\value{
A named vector of biomass loss rate due to external mortality for
each species
}
\description{
For each species, returns the rate at which biomass is lost due to external
mortality.
}
\details{
This is calculated as
\deqn{M0B_i = \int_{w_0}^{w_{max}} \mu_{ext.i}(w) N_i(w) w dw}
where \eqn{\mu_{ext.i}(w)} is the external mortality rate of an individual
of species \eqn{i} and weight \eqn{w} (obtained with \code{\link{getExtMort}})
and \eqn{N_i(w)} is the number density of species i at weight w.
}
\section{Discretisation}{

The integral is evaluated on mizer's finite-volume size grid as
\eqn{\sum_j N_{ij}\bar K_{ij}\Delta w_j}, where \eqn{K_i(w)} is everything in
the integrand except the number density \eqn{N_i(w)}, including any
size-window mask coming from the \code{min_w}/\code{max_w} arguments. By default
\eqn{\bar K_{ij}} is the value of \eqn{K_i} at the left edge of the bin,
which is what mizer's own summary functions use. If the model has bin
averaging switched on with \code{\link[mizer:second_order_w]{mizer::second_order_w()}}, then \eqn{\bar K_{ij}}
is instead the trapezoidal average of \eqn{K_i} over the bin, making the
result second order in the bin width and consistent with the quadratures
mizer uses in that case.
}

\examples{
getM0B(NS_params)
}
\seealso{
Other rate functions:
\code{\link[=getConsumption]{getConsumption()}},
\code{\link[=getDietMatrix]{getDietMatrix()}},
\code{\link[=getEcotrophicEfficiency]{getEcotrophicEfficiency()}},
\code{\link[=getGonadicProduction]{getGonadicProduction()}},
\code{\link[=getM2B]{getM2B()}},
\code{\link[=getMetabolicRespiration]{getMetabolicRespiration()}},
\code{\link[=getOffspringProduction]{getOffspringProduction()}},
\code{\link[=getProduction]{getProduction()}},
\code{\link[=getReproductiveEfficiency]{getReproductiveEfficiency()}},
\code{\link[=getReproductiveLoss]{getReproductiveLoss()}},
\code{\link[=getRespiration]{getRespiration()}},
\code{\link[=getSomaticProduction]{getSomaticProduction()}},
\code{\link[=getTotalProduction]{getTotalProduction()}},
\code{\link[=getUnassimilated]{getUnassimilated()}},
\code{\link[=getZB]{getZB()}}
}
\concept{rate functions}
