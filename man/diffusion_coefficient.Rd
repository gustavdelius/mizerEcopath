% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diffusion_params.R
\name{diffusion_coefficient}
\alias{diffusion_coefficient}
\title{Coefficient of the external-diffusion power law for one species}
\usage{
diffusion_coefficient(params, sp_select, n)
}
\arguments{
\item{params}{A MizerParams object.}

\item{sp_select}{A logical vector selecting a single species.}

\item{n}{The exponent of the growth power law for that species.}
}
\value{
A single numeric value, the \code{D_ext} coefficient.
}
\description{
The growth-diffusion rate is modelled as a power law
\deqn{d(w) = D_{ext}\, w^{n+1},}
stored in the \code{ext_diffusion} slot of the \link[mizer:MizerParams]{MizerParams}
object. This helper returns the coefficient \code{D_ext} for a single species.
}
\details{
The \code{ext_diffusion} slot is the diffusion that the model dynamics actually
use, so it is treated as the source of truth: when it is non-zero the
coefficient is read off the stored rate. Otherwise the value is taken from the
\code{D_ext} species parameter, or, failing that, from the legacy \code{d_over_g}
parameter via \eqn{D_{ext} = (d/g)\, g_0} with \eqn{g_0 = g(w_1)/w_1^{n}}.
When no diffusion information is available it returns 0.
}
\keyword{internal}
