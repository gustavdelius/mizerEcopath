% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get.R
\name{getReproductiveEfficiency}
\alias{getReproductiveEfficiency}
\title{Get the efficiency of reproduction}
\usage{
getReproductiveEfficiency(object)
}
\arguments{
\item{object}{A MizerParams or MizerSim object}
}
\value{
If called with a MizerParams object, a named vector with the
reproductive efficiency of each species in the model. If called with a
MizerSim object, an \code{ArrayTimeBySpecies} object (time x species) with the
reproductive efficiency at each saved time. The values are dimensionless
fractions.
}
\description{
For each species, returns the fraction of the energy invested into
reproduction that arrives in the size spectrum as offspring biomass. For a
\code{MizerParams} object this is calculated from the initial state, for a
\code{MizerSim} object it is returned for every saved time.
}
\details{
Mature individuals invest energy into reproduction at the total rate
\eqn{E_{R.i}} (grams per year), obtained with \code{\link[=getGonadicProduction]{getGonadicProduction()}}.
Mizer assumes that half of that investment is made by females, so
\eqn{E_{R.i} / 2} is what is available for making eggs. Offspring biomass is
produced at the rate \eqn{R_{dd.i} w_{0.i}}, obtained with
\code{\link[=getOffspringProduction]{getOffspringProduction()}}. The reproductive efficiency is the ratio of the
two:
\deqn{\frac{R_{dd.i}\, w_{0.i}}{E_{R.i} / 2} =
      \epsilon_i \, \frac{R_{dd.i}}{R_{di.i}},}
where \eqn{\epsilon_i} is the \code{erepro} species parameter and
\eqn{R_{di.i}} is the density-independent rate of egg production, see
\code{\link[mizer:getRDI]{mizer::getRDI()}}. The second factor \eqn{R_{dd.i}/R_{di.i}} is the fraction
of eggs that survives density dependence and equals
\code{1 - reproduction_level}, see \code{\link[mizer:setBevertonHolt]{mizer::setBevertonHolt()}}.

The function evaluates the right-hand side of the above equation, because
that is the form mizer itself uses when it projects reproduction. The two
sides agree exactly, whatever \code{\link[mizer:second_order_w]{mizer::second_order_w()}} is set to, because
\code{\link[=getGonadicProduction]{getGonadicProduction()}} discretises the reproduction integral with the same
quadrature that \code{\link[mizer:getRDI]{mizer::getRDI()}} uses.

A value above 1 is energetically impossible: the model would be producing
more offspring biomass than the adults invested. Use
\code{\link[=plotReproductiveEfficiency]{plotReproductiveEfficiency()}} to see how far each species is from that
limit and how much of the loss is due to density dependence. Species that
do not reproduce at all (\eqn{R_{di.i} = 0}) get \code{NA}.
}
\examples{
getReproductiveEfficiency(celtic_params)
}
\seealso{
\code{\link[=plotReproductiveEfficiency]{plotReproductiveEfficiency()}}, \code{\link[=getOffspringProduction]{getOffspringProduction()}},
\code{\link[=getGonadicProduction]{getGonadicProduction()}}

Other rate functions:
\code{\link[=getConsumption]{getConsumption()}},
\code{\link[=getDietMatrix]{getDietMatrix()}},
\code{\link[=getEcotrophicEfficiency]{getEcotrophicEfficiency()}},
\code{\link[=getGonadicProduction]{getGonadicProduction()}},
\code{\link[=getM0B]{getM0B()}},
\code{\link[=getM2B]{getM2B()}},
\code{\link[=getMetabolicRespiration]{getMetabolicRespiration()}},
\code{\link[=getOffspringProduction]{getOffspringProduction()}},
\code{\link[=getProduction]{getProduction()}},
\code{\link[=getReproductiveLoss]{getReproductiveLoss()}},
\code{\link[=getRespiration]{getRespiration()}},
\code{\link[=getSomaticProduction]{getSomaticProduction()}},
\code{\link[=getTotalProduction]{getTotalProduction()}},
\code{\link[=getUnassimilated]{getUnassimilated()}},
\code{\link[=getZB]{getZB()}}
}
\concept{rate functions}
