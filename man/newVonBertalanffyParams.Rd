% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/newVonBertalanffyParams.R
\name{newVonBertalanffyParams}
\alias{newVonBertalanffyParams}
\title{Create model with von Bertalanffy growth rates in the steady state}
\usage{
newVonBertalanffyParams(species_params, no_w = 200, max_w = NULL)
}
\arguments{
\item{species_params}{Species parameter data frame. Must contain the von
Bertalanffy parameters \code{w_inf}, \code{k_vb} and \code{t0} as well as the usual mizer
parameters such as \code{b}, \code{w_mat} and \code{w_max}.}

\item{no_w}{Number of size bins. Default 200}

\item{max_w}{Largest size in the model. Defaults to the largest \code{w_max}.}
}
\value{
A MizerParams object with the model at steady state
}
\description{
Builds a single- or multi-species \link[mizer:MizerParams-class]{MizerParams}
object in which each species grows along its von Bertalanffy curve while at
steady state. The von Bertalanffy parameters are supplied in the \code{w_inf},
\code{k_vb} and \code{t0} columns of \code{species_params}, together with the weight-length
exponent \code{b}, the maturity size \code{w_mat} and the maximum size \code{w_max}.
}
\section{Growth}{

The von Bertalanffy growth in length, \eqn{dl/dt = k_{vb}(L_\infty - l)},
becomes in weight \eqn{dw/dt = A\,w^n - B\,w} with \eqn{n = 1 - 1/b},
\eqn{A = b\,k_{vb}\,w_\infty^{1/b}} and \eqn{B = b\,k_{vb}} (see
\code{\link[=vonBertalanffyGrowth]{vonBertalanffyGrowth()}}). The model reproduces this rate by
\itemize{
\item switching off satiation, predation and resource interactions so that
the energy income is a pure power law \eqn{\alpha E = A\,w^n},
\item setting the metabolic loss rate to \eqn{B\,w} (so
\eqn{\alpha E - \mathrm{metab} = A\,w^n - B\,w}), and
\item switching off constant mortality and using a power-law external
mortality chosen to give the juvenile biomass density a slight negative
slope.
}
}

\section{Reproduction}{

Mizer computes somatic growth as \eqn{g = (1 - \psi)(\alpha E -
\mathrm{metab})}, where \eqn{\psi} is the fraction of surplus energy invested
into reproduction. For the \emph{somatic} growth to stay on the von Bertalanffy
curve the surplus energy must therefore be \eqn{g_{vb}/(1 - \psi)}, so the
metabolic loss is reduced by \eqn{\psi/(1 - \psi)\,g_{vb}}. Because
\code{w_repro_max} is set to \code{w_inf}, and mizer's maturity ogive gives
\eqn{\psi \propto (w/w_{\mathrm{repro\,max}})^{1-n}} above \code{w_mat}, this
reduction equals the metabolic loss itself at \code{w_inf} (all income then funds
reproduction) and is smaller below, so the metabolic loss stays
non-negative.
}

\section{Negative \code{t0}}{

A negative \code{t0} means the von Bertalanffy fish already has a positive size at
age 0, so mizer, which starts every fish at \code{w_min}, cannot follow the curve
below maturity. Instead the growth below maturity is sped up so that maturity
is reached at the age the von Bertalanffy curve (with its negative \code{t0})
predicts, namely \eqn{t_{\mathrm{mat}} = t_0 - \log(1 -
(w_{\mathrm{mat}}/w_\infty)^{1/b})/k_{vb}}; above maturity the growth is left
unchanged and so rejoins the von Bertalanffy curve. The speed-up is applied
in two stages:
\itemize{
\item The metabolic loss below maturity is scaled by a constant factor
\eqn{c \in (0, 1]}, which keeps the growth a von Bertalanffy form with
rate \eqn{c\,k_{vb}} and asymptote \eqn{w_\infty^{1/b}/c}. The factor is
found by inverting the resulting age-at-maturity for \code{c}.
\item Removing the metabolic loss entirely (\eqn{c = 0}) gives the fastest
growth this can achieve and hence a minimum reachable maturity age. If
\code{t0} is so negative that even this is too slow, the metabolic loss is set
to zero and the intake (external encounter rate) below maturity is
additionally raised by the factor needed to hit the target maturity age.
A message reports the factor used. Note that this raises the implied
juvenile consumption above the power law for the affected species.
}
Species with \code{t0 >= 0} (or missing \code{t0}) are left unchanged.
}

\seealso{
\code{\link[=vonBertalanffyGrowth]{vonBertalanffyGrowth()}}
}
